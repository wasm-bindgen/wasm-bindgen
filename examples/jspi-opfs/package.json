{
  "scripts": {
    "build": "cross-env RUSTFLAGS=--cfg=js_sys_unstable_apis wasm-pack build --target web --out-dir ../dist/jspi-opfs",
    "postbuild": "node build-post.mjs"
  },
  "devDependencies": {
    "cross-env": "catalog:"
  }
}
