{
  "scripts": {
    "pretest": "cargo build -p wasm-bindgen-cli --bin wasm-bindgen",
    "test": "playwright test"
  },
  "dependencies": {
    "@playwright/test": "^1.55.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
