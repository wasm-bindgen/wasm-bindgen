error: `slice_to_array` cannot be applied to a `&mut` slice: it hands JS an owned `Array` copied out of linear memory, so anything JS writes into that `Array` is discarded rather than written back into the caller's slice.

       Either change this argument to `&[u16]`, if JS only needs to read the elements, or remove `slice_to_array` (from this argument, from this function, or from the enclosing `extern "C"` block, wherever it is set) so the slice keeps the writable typed-array view that `&mut` normally gets.
  --> ui-tests/slice-to-array-mut.rs:17:51
   |
17 |     fn direct(#[wasm_bindgen(slice_to_array)] xs: &mut [u16]);
   |                                                   ^

error: `slice_to_array` cannot be applied to a `&mut` slice: it hands JS an owned `Array` copied out of linear memory, so anything JS writes into that `Array` is discarded rather than written back into the caller's slice.

       Either change this argument to `&[u32]`, if JS only needs to read the elements, or remove `slice_to_array` (from this argument, from this function, or from the enclosing `extern "C"` block, wherever it is set) so the slice keeps the writable typed-array view that `&mut` normally gets.
  --> ui-tests/slice-to-array-mut.rs:21:19
   |
21 |     fn per_fn(xs: &mut [u32]);
   |                   ^

error: `slice_to_array` cannot be applied to a `&mut` slice: it hands JS an owned `Array` copied out of linear memory, so anything JS writes into that `Array` is discarded rather than written back into the caller's slice.

       Either change this argument to `Option<&[f64]>`, if JS only needs to read the elements, or remove `slice_to_array` (from this argument, from this function, or from the enclosing `extern "C"` block, wherever it is set) so the slice keeps the writable typed-array view that `&mut` normally gets.
  --> ui-tests/slice-to-array-mut.rs:26:21
   |
26 |     fn optional(xs: Option<&mut [f64]>);
   |                     ^^^^^^

error: `slice_to_array` cannot be applied to a `&mut` slice: it hands JS an owned `Array` copied out of linear memory, so anything JS writes into that `Array` is discarded rather than written back into the caller's slice.

       Either change this argument to `&[u8]`, if JS only needs to read the elements, or remove `slice_to_array` (from this argument, from this function, or from the enclosing `extern "C"` block, wherever it is set) so the slice keeps the writable typed-array view that `&mut` normally gets.
  --> ui-tests/slice-to-array-mut.rs:32:41
   |
32 |     fn mixed(readable: &[u8], writable: &mut [u8]);
   |                                         ^

error: `slice_to_array` cannot be applied to a `&mut` slice: it hands JS an owned `Array` copied out of linear memory, so anything JS writes into that `Array` is discarded rather than written back into the caller's slice.

       Either change this argument to `&[i32]`, if JS only needs to read the elements, or remove `slice_to_array` (from this argument, from this function, or from the enclosing `extern "C"` block, wherever it is set) so the slice keeps the writable typed-array view that `&mut` normally gets.
  --> ui-tests/slice-to-array-mut.rs:40:22
   |
40 |     fn per_block(xs: &mut [i32]);
   |                      ^
